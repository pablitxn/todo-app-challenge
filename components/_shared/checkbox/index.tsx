import React, { FC } from 'react'
import { ICheckbox } from './types'
import StyledCheckbox from './styles'

const Checkbox: FC<ICheckbox> = ({ onClick }) => {
  // onClick()

  return <StyledCheckbox />
}

export default Checkbox
